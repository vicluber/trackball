/ {
    chosen {
        zmk,kscan = &kscan0;
    };

    kscan0: kscan_0 {
        compatible = "zmk,kscan-gpio-direct";
        input-gpios
            = <&gpio0 20 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>   /* UP    */
            , <&gpio0 22 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>   /* DOWN  */
            , <&gpio1 15 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>   /* LEFT  */
            , <&gpio0 24 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>   /* RIGHT */
            , <&gpio0 2  (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>   /* CLICK */
            ;
        debounce-press-ms = <1>;
        debounce-release-ms = <1>;
    };
};
